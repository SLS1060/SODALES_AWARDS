_schema-version: 3.3.0
ID: SODALESAWARDS
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: SODALESAWARDS-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      memory: 256M
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: SODLS_INS_INTASS-db
      - name: SODALESAWARDS-auth
      - name: Product1_CPIDestination

  - name: SODLS_INS_INTASS-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
      memory: 100M
    requires:
      - name: SODLS_INS_INTASS-db

resources:
  - name: SODLS_INS_INTASS-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: SODALESAWARDS-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: SODALESAWARDS-Sodales-Dev
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
  - name: Product1_CPIDestination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
